extends RichTextLabel

class_name LogBox

# In your UI script
@onready var log_box = $MyLogLabel 

func _ready():
	pass
# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
